context:
  name: easydiffraction
  version: "0.10.1"
  python_min: "3.11"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/easydiffraction-${{ version }}.tar.gz
  sha256: 5eed129eabdd0680d24421c9c1bd10ade7918f453fe4d9f2a0b07482c33a52d5

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - versioningit
    - pip
  run:
    - python >=${{ python_min }},<3.14
    - numpy
    - colorama
    - tabulate
    - asciichartpy
    - pooch
    - typer
    - rich
    - varname
    - asteval
    - scipy
    - sympy
    - lmfit
    - bumps
    - dfo-ls
    - gemmi
    - cryspy
    - diffpy.pdffit2
    - diffpy.utils
    - uncertainties
    - typeguard
    # visualization optional deps
    - darkdetect
    - pandas
    - py3Dmol
    - plotly

tests:
  - python:
      imports:
        - easydiffraction
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://easydiffraction.org/
  summary: Making diffraction data analysis and modelling easy
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - MridulS
